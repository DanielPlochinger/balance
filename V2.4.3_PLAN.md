# Balance v2.4.3 — bugfix release

**Status: BIG PICTURE (collecting bugs) — nothing locked, nothing executed.**

Workflow: big picture → details honed with Daniel → single execution pass on
his explicit "build". This doc stays forever as the record of what 2.4.3 fixed,
including the troubleshooting trail.

---

## 1. Candidate bugs (unconfirmed — Daniel to confirm, strike, or add)

- **1a — Bottom sheets misbehave.** Daniel (2026-08-08): the cards coming up
  from the bottom are inconsistent and "some stuff doesn't work with them."
  Symptoms not yet enumerated — need Daniel's list or a repro session
  (candidates to probe: tap-outside-to-close, keyboard covering inputs,
  scroll-inside-sheet, grabber doing nothing, background scrolling behind
  the sheet).
- **1b — Possible sixth food visible.** Daniel counted six foods on
  2026-08-15; the database holds five live + one tombstone (bad "Beer,
  220 kcal per item"). If the Foods tab shows Beer twice on v2.4.2 with a
  green sync dot, the tombstone filter leaks somewhere. Awaiting Daniel's
  confirmation — may be a non-bug (miscount).
- **1c — Deposit cap surprise.** Config carries `cap: 1000` (pre-wipe
  leftover, not the 300 default). Not a code bug — flagged so Daniel decides:
  keep 1000 or reset to 300. Settings-only change, no code.

## 2. Diagnosis notes

*(filled during troubleshooting, before any fix is coded)*

## 3. Agreed fixes

*(locked with Daniel before execution)*

## 4. Testing checklist

*(derived from §3 when it locks)*

## 5. Shipped

*(commit, tag, and deploy notes land here after the build)*
