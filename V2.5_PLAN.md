# Balance v2.5 — remote truth

Status: LOCKED 2026-08-16. Awaiting Daniel's explicit go; this doc alone must suffice to build from.

## 1. Architecture: the repo is the only truth

- Every read and write goes to `balance-data/state.json`. There is no other authoritative copy, ever.
- Local storage becomes a **display cache** only: render last-known data instantly on open, then refresh from remote. The cache is never merged back into anything.
- Writes are **remote-first**: fetch fresh state → apply the single mutation → PUT with version check (sha). On conflict: refetch, reapply the same mutation, retry (max 3). The UI confirms a log only after the repo accepts it.
- Offline = **read-only**: sync pill shows "offline"; Log with Claude / Manual / weight Save / food Add render disabled; tapping any shows toast "Offline — logging needs a connection." Everything cached stays readable; controls re-enable automatically when the next fetch succeeds. No write queues.
  *Intent: a second truth must never exist, not even temporarily — queues are a local truth through the side door.*

## 2. Machinery deleted

- The merge engine (`mergeState`), the content-guard, food and entry tombstones, the `wipedAt` purge system, lazy `stampDays()` and all its ordering rules. This entire bug class dies with the copies it protected.
- Migration physically removes tombstoned foods from state (the buried Beer disappears for real).
- Future data wipes = edit the single truth directly (+ history erase per the deletion policy in CLAUDE.md).

## 3. Settings snapshots — kept, race-free

- Per-day snapshots `b/c/g/m` remain the rule (settings-of-the-day; closed days immutable).
- They are written onto the **open day** as part of every remote write that touches it (entry, weight, settings save). When the day passes it is already complete. No lazy stamping exists anywhere.

## 4. Settings save: no invented values

- An empty or invalid Settings field keeps its previous value. The app never substitutes a default on save.
- First-run seed defaults remain (visible in Settings, replaced by the first save).

## 5. Migration & data corrections (one-time)

- Restore to Aug 15: entry "Pate", 23:17, 270 kcal (erased by the stale-stamp clobber).
- Fix Aug 15 snapshot cap: 300 → 1,000 (the value actually in force that day).
- Stamp any day missing its `b/c/g/m` snapshot with current config (covers the open day so it can never close unstamped).
- Delete the orphan `wipedAt` key from config; physically remove tombstoned foods.
- The bank needs no manual correction — it is derived, and recomputes from the corrected inputs: Aug 15 deposit = min(1,900 − 1,327, 1,000) = **573**.
- **Sequencing**: deploy v2.5 → Daniel relaunches BOTH devices (verify v2.5.0 in Settings) → only then run this migration → verify state. Old clients must never write after migration (their merge engine could re-introduce deleted fields).

## 6. Sync pill semantics

- `live` (green — remote confirmed) · `saving` · `offline` (read-only) · `error`. No "local" state exists anymore.
- Built by remapping the existing pill component and its dot classes (green/amber/red/default-grey) — no new CSS, no second indicator. Boot shows grey "connecting" until the first fetch resolves. Demo keeps its "demo" label.

## 7. Implementation map

- Replace pull/push/scheduleSync with two functions: `fetchState()` (GET + sha) and `write(mutator)` (fetch → mutate → PUT sha, retry ≤ 3, then re-render).
- All mutations funnel through `write()`: addEntries, delEntry, saveWeight, food CRUD incl. toggleFav, saveSettings, toast-Undo, §8 restore. Each is a small pure mutator so conflict-retry reapplication is trivially safe.
- **One user action = one remote write.** The log mutator carries its "save to my foods" items inside it (no separate library write). Success toast fires only after the PUT confirms.
- In-flight writes serialize (next mutator waits, then applies to a fresh fetch). This is not an offline queue — nothing persists, and offline still blocks writes (§1).
- `ghUrl()` generalizes to `ghUrl(path)` (`state.json`, `backups/…`); `ghHeaders`/`ghReady` reused as-is — no parallel GitHub helpers.
- Tombstone-era code goes entirely: `foodDel` deletes for real; the `activeFoods()` filter wrapper is removed, call sites use `S.foods` directly.
- Boot: render from cache → `fetchState()` → re-render. Inputs disabled in offline state.
- "Test connections" (Settings) repoints its retry to `fetchState()`/`write()`.
- Secrets stay device-local as today. Demo mode stays fully local (stubbed remote), remains the test harness.
- `APP_VERSION` 2.5.0; sw `CACHE` bump; tag `v2.5`; rewrite CLAUDE.md architecture section (remove merge-strategy notes, describe remote-truth); prune resolved BACKLOG items.

## 8. Backups: repo snapshots

- Create backup (Settings, one tap) → current state copied to `backups/<date-time>.json` in the data repo. No device files.
- Restore → sheet lists snapshots by date/time, newest first → pick → confirm → snapshot replaces current state **through the normal `write()` path**.
- Paste-import is removed (it was a second truth through the side door). One-way file export stays as a portability escape hatch.
  *Intent: backups live inside the single truth; restoring is just another write.*

## 9. Testing checklist

- Two-client simulation: alternate writes from two contexts → all entries survive; no clobber path exists.
- Conflict: concurrent writes → retry resolves, both mutations present.
- Offline: blocked network → read-only pill, logging disabled; clean recovery on reconnect.
- Corrections: Aug 15 shows 8 entries incl. Pate 270; bank reads +573 (before today's close).
- Snapshots: mid-day settings change writes to the open day only; closed days byte-identical before/after.
- Backup: create → appears in restore list; restore → state matches snapshot; export still downloads.
- Migration: unstamped days stamped; `wipedAt` gone; tombstoned foods physically absent; exactly five foods remain.
- Offline taps: disabled controls show the offline toast.
- One-write rule measured: a log action including a saved food = exactly one new commit in the data repo.
- Two rapid quick-logs: both entries present, two commits, zero losses.
- Demo exercises all §9 paths.
