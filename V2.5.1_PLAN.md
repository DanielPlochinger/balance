# Balance v2.5.1

Status: in discussion. Execute only on Daniel's explicit go; this doc alone must suffice to build from.

## 1 — Historic day view: tap a bar, get the day

**Change — LOCKED 2026-08-16**

- Tapping a 14-day bar opens the existing bottom sheet (`sheet()`) with that day instead of the value chip.
- Weight dots keep their value chip (Daniel: weight tap = the value; calorie tap = the day). The chip component stays, used by the weight chart only.
- Sheet content, top to bottom: date as title · "eaten X of Y" header · settlement badge (**+N banked** green / **−N debt** red / **covered by bank** blue) · the day's entries in the same rows as Today's log (blue negatives) · weight if logged.
- Ledger extension: the replay additionally returns per-day settlement results (deposit / withdrawal / debt added) — same math, one extra output; no new persisted data.
  *Intent: reuse everything — the sheet, the entry rows, the existing tap. History appears where curiosity strikes; no new surface, no navigation.*

## 2 — One ghost-grey for not-yet-input values

**Change — LOCKED 2026-08-16**

- New single token `--ghost` (light grey between muted text and borders).
- Applied to BOTH input placeholders (`::placeholder`, currently browser-default) and the rest-state weight value.
  *Intent: one grey means "value not entered/committed" everywhere; no browser-chosen defaults.*
